# hello-automation

Run the app from the project root with:

```powershell
uv run python main.py
```

If you want to call the package entrypoint directly, use:

```powershell
uv run python -m hello_automation
```
